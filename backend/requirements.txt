fastapi==0.115.0
uvicorn[standard]==0.30.0
python-jose[cryptography]==3.3.0
cryptography==43.0.0
# torch is intentionally omitted here — installed separately in start.sh
# to pick up the CUDA-enabled wheel from the RunPod system or PyTorch index.
# If running locally without GPU: pip install torch
transformers==4.46.0
accelerate==0.34.0
bitsandbytes>=0.46.1
sentencepiece
protobuf
captum==0.7.0
pydantic==2.8.0
pytest==8.3.0
pytest-asyncio==0.23.0
httpx==0.27.0
